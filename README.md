# van-goghizer
Trying some image processing stuff in javascript. 
This program will load an image input by the user, then draw a version of that picture on a canvas, but only with ellipses 
and using a van gogh's painting's color palette as the ellipses colors.
